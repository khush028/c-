// keyword = is resrved word whose meaning already defined on the complier
//  example =  for  = 2;(not allowed)
// we can't use keyword as a variable and constant name
// keyword must bi in lowercase

#include<iostream>
using namespace std;
main()
{
    //int for = 10;
    //cout<<for;
}